services:
  - type: web
    name: restaurant-bot
    env: python
    region: frankfurt
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: BOT_TOKEN
        sync: false

databases:
  - name: finance-db
    databaseName: finance_db
    user: finance_user
